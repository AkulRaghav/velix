name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  release-backend:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        service: [routing, identity, media, push, call, notifier]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build, push, sign, SBOM
        env:
          SOURCE_DATE_EPOCH: ${{ github.event.head_commit.timestamp }}
          TAG: ${{ github.ref_name }}
        run: |
          docker buildx build \
            --build-arg SOURCE_DATE_EPOCH="$SOURCE_DATE_EPOCH" \
            --build-arg GIT_REVISION="${{ github.sha }}" \
            --provenance=true \
            --sbom=true \
            --tag ghcr.io/velix/${{ matrix.service }}:${TAG} \
            --tag ghcr.io/velix/${{ matrix.service }}:${{ github.sha }} \
            --push \
            -f backend/services/${{ matrix.service }}/Dockerfile \
            backend/
      - uses: sigstore/cosign-installer@v3
      - name: cosign sign
        run: cosign sign --yes ghcr.io/velix/${{ matrix.service }}:${{ github.ref_name }}

  release-flutter:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.x'
      - working-directory: apps/velix_app
        run: |
          flutter pub get
          flutter build appbundle --release
          flutter build ipa --release --no-codesign

  github-release:
    runs-on: ubuntu-22.04
    needs: [release-backend, release-flutter]
    steps:
      - uses: actions/checkout@v4
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          prerelease: ${{ contains(github.ref, 'rc') }}
