name: velix_design
description: Velix design system reference. Tokens, themes, motion, materials. Consumed by velix_app.
version: 1.0.0-phase2
publish_to: none

environment:
  sdk: ^3.4.0
  flutter: ^3.22.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: false
