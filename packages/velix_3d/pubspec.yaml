name: velix_3d
description: Velix 3D experience system. Renderer controller, scene definitions, fallback policy. Wraps Filament via FFI in Phase 5.
version: 1.0.0-phase3
publish_to: none

environment:
  sdk: ^3.4.0
  flutter: ^3.22.0

dependencies:
  flutter:
    sdk: flutter
  velix_design:
    path: ../velix_design

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: false
