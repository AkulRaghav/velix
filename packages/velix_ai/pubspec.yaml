name: velix_ai
description: Velix AI layer. On-device-first inference; cloud invocation only via OHTTP relay with per-query consent.
version: 1.0.0-phase8
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  meta: ^1.12.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
